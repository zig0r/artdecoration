export function sendEmail(vars) {
  // return new Promise((resolve, reject) => {
  //   const xhr = new XMLHttpRequest();

  //   xhr.open('POST', 'https://api.mailjet.com/v3.1/send', username, password);
  //   xhr.setRequestHeader('Content-Type', 'application/json');
  //   xhr.onload = () => resolve(JSON.parse(xhr.responseText));
  //   xhr.ontimeout = xhr.onerror = reject; // eslint-disable-line no-multi-assign
  //   xhr.send(JSON.stringify(createEmail(vars)));
  // });
}
